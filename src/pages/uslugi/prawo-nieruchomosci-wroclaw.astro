---
import { getEntry, render } from 'astro:content';
import Layout from '@components/layout/Layout.astro';
import ServiceHero from '@components/ui/ServiceHero.astro';
import FAQAccordion from '@components/ui/FAQAccordion.astro';
import Button from '@components/ui/Button.astro';
import StructuredData from '@components/seo/StructuredData.astro';

const serviceEntry = await getEntry('uslugi', 'prawo-nieruchomosci-wroclaw');
const data = serviceEntry?.data;
const renderResult = serviceEntry ? await render(serviceEntry) : null;
const Content = renderResult ? renderResult.Content : null;

const serviceSchema = {
  "@context": "https://schema.org", "@type": "LegalService",
  "name": "Prawo nieruchomości Wrocław — Kancelaria ABW",
  "url": "https://kancelariaabw.pl/uslugi/prawo-nieruchomosci-wroclaw",
  "telephone": "+48609366160",
  "description": data?.seo?.description || "Obsługa prawna w sprawach nieruchomości we Wrocławiu",
  "areaServed": ["Wrocław", "Dolny Śląsk"]
};
const faqSchema = {
  "@context": "https://schema.org", "@type": "FAQPage",
  "mainEntity": (data?.faq || []).map(f => ({ "@type": "Question", "name": f.question, "acceptedAnswer": { "@type": "Answer", "text": f.answer } }))
};
const breadcrumb = {
  "@context": "https://schema.org", "@type": "BreadcrumbList",
  "itemListElement": [
    { "@type": "ListItem", "position": 1, "name": "Strona główna", "item": "https://kancelariaabw.pl" },
    { "@type": "ListItem", "position": 2, "name": "Usługi", "item": "https://kancelariaabw.pl/uslugi" },
    { "@type": "ListItem", "position": 3, "name": data?.hero?.title || "Nieruchomości", "item": "https://kancelariaabw.pl/uslugi/prawo-nieruchomosci-wroclaw" },
  ]
};
---

<Layout
  title={data?.title || "Prawo Nieruchomości"}
  description={data?.seo?.description || ""}
>
  <Fragment slot="head">
    <StructuredData schema={serviceSchema} />
    <StructuredData schema={faqSchema} />
    <StructuredData schema={breadcrumb} />
  </Fragment>

  <ServiceHero
    title={data?.hero?.title || ""}
    subtitle={data?.hero?.subtitle || undefined}
    description={data?.hero?.description || undefined}
    label="Nieruchomości"
    ctaText="Umów konsultację"
    image="/photos/hero_nieruchomosci_1772972108703.png"
    imageAlt="Prawo nieruchomości radca prawny"
  />

  <section class="section bg-white">
    <div class="w-full max-w-7xl mx-auto px-6 lg:px-8 max-w-5xl">
      <div class="grid grid-cols-1 lg:grid-cols-3 gap-12">
        <article class="lg:col-span-2 prose prose-navy max-w-none">
          {Content && <Content />}
        </article>
        <aside>
          <div class="bg-navy rounded-2xl p-6 sticky top-6">
            <p class="font-sans text-xs text-white opacity-60 uppercase tracking-widest mb-3">Nieruchomości</p>
            <p class="font-serif text-xl text-white mb-4">Lokator nie płaci lub nie wychodzi?</p>
            <Button href="/kontakt" variant="gold" class="w-full justify-center">Umów pilną konsultację →</Button>
            <div class="mt-4 pt-4 border-t border-white/10">
              <p class="font-sans text-xs text-white opacity-50 mb-2">Przydatne narzędzia</p>
              <a href="/narzedzia/przedawnienie-roszczen" class="font-sans text-sm text-white opacity-70 hover:text-white block py-1">→ Kalkulator przedawnienia</a>
              <a href="/narzedzia/oplaty-sadowe" class="font-sans text-sm text-white opacity-70 hover:text-white block py-1">→ Kalkulator opłat sądowych</a>
            </div>
          </div>
        </aside>
      </div>
    </div>
  </section>

  <FAQAccordion items={data?.faq || []} title="Pytania o prawo nieruchomości" />

  <section class="section bg-navy">
    <div class="w-full max-w-7xl mx-auto px-6 lg:px-8 max-w-3xl text-center">
      <h2 class="font-serif text-3xl text-white mb-4">Problem z nieruchomością?</h2>
      <p class="font-sans text-white opacity-70 mb-8 max-w-lg mx-auto">Skontaktuj się z nami — szybko ocenimy sytuację prawną i zaproponujemy najlepsze wyjście.</p>
      <Button href="/kontakt" variant="gold" size="lg">Umów konsultację</Button>
    </div>
  </section>
</Layout>
