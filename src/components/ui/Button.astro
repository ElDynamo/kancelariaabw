---
interface Props {
  href: string;
  variant?: 'primary' | 'outline' | 'ghost';
  size?: 'sm' | 'md' | 'lg';
  class?: string;
}

const {
  href,
  variant = 'primary',
  size = 'md',
  class: extraClass = '',
} = Astro.props;

const base = 'inline-flex items-center justify-center font-sans uppercase tracking-wider transition-colors duration-200 rounded-lg focus:outline-none focus:ring-2 focus:ring-gold focus:ring-offset-2';
const sizes = { sm: 'text-xs px-4 py-2', md: 'text-sm px-6 py-3', lg: 'text-base px-8 py-4' };
const variants = {
  primary: 'bg-navy text-white hover:bg-navy-dark',
  outline: 'border-2 border-navy text-navy hover:bg-navy hover:text-white',
  ghost: 'text-navy hover:text-gold underline-offset-4 hover:underline',
};
---
<a
  href={href}
  class:list={[base, sizes[size], variants[variant], extraClass]}
>
  <slot />
</a>
